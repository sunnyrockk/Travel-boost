import { Link, useNavigate } from "react-router-dom";
import { useState } from "react";
import { useAuth } from "../context/AuthContext";

export default function Topbar() {
  const { user, logout } = useAuth();
  const navigate = useNavigate();
  const [menu, setMenu] = useState(null);
  const [query, setQuery] = useState("");
  const [currency, setCurrency] = useState(() => localStorage.getItem("tb_currency") || "INR");
  const chooseCurrency = (value) => { localStorage.setItem("tb_currency", value); setCurrency(value); setMenu(null); };
  const signOut = () => { logout(); setMenu(null); navigate("/"); };
  return <header className="flex items-center gap-4 px-6 py-4 border-b border-gray-100 bg-white"><form onSubmit={(event) => { event.preventDefault(); navigate(`/explore?destination=${encodeURIComponent(query)}`); }} className="flex-1 max-w-md relative"><span className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400">🔍</span><input value={query} onChange={(event) => setQuery(event.target.value)} type="text" placeholder="Where do you want to go?" className="w-full pl-9 pr-3 py-2.5 rounded-lg border border-gray-200 text-sm focus:outline-none focus:ring-2 focus:ring-brand-300" /></form><div className="ml-auto flex items-center gap-3 text-sm relative"><div className="relative"><button onClick={() => setMenu(menu === "currency" ? null : "currency")} className="rounded-lg px-2 py-2 text-gray-600 hover:bg-gray-50">{currency} ▾</button>{menu === "currency" && <div className="absolute right-0 z-30 mt-2 w-52 rounded-xl border border-gray-100 bg-white p-2 shadow-lg"><p className="px-2 py-1 text-xs text-gray-400">Display currency</p><button onClick={() => chooseCurrency("INR")} className="w-full rounded-lg px-3 py-2 text-left text-sm hover:bg-brand-50">₹ Indian Rupee (INR) {currency === "INR" && "✓"}</button><p className="px-2 pt-2 text-[11px] text-gray-400">All demo prices are shown in INR.</p></div>}</div><div className="relative"><button onClick={() => setMenu(menu === "notifications" ? null : "notifications")} aria-label="Notifications" className="rounded-lg p-2 text-lg hover:bg-gray-50">🔔</button>{menu === "notifications" && <div className="absolute right-0 z-30 mt-2 w-72 rounded-xl border border-gray-100 bg-white p-3 shadow-lg"><p className="mb-2 font-semibold text-gray-800">Notifications</p>{user ? <><div className="rounded-lg bg-brand-50 p-3 text-xs text-brand-800">Welcome, {user.name}. Your bookings and local-tourism offers are ready to manage.</div><Link onClick={() => setMenu(null)} to="/bookings" className="mt-2 block text-xs font-medium text-brand-600">View my bookings →</Link></> : <p className="text-xs text-gray-500">Sign in to receive booking updates and travel reminders.</p>}</div>}</div>{user ? <div className="relative"><button onClick={() => setMenu(menu === "profile" ? null : "profile")} className="flex items-center gap-2 rounded-lg px-2 py-1.5 hover:bg-gray-50"><span className="w-8 h-8 rounded-full bg-brand-100 text-brand-700 flex items-center justify-center font-semibold">{user.name?.[0]?.toUpperCase()}</span><span className="hidden sm:block text-left"><span className="block text-gray-800 font-medium leading-none">Hi, {user.name.split(" ")[0]}</span><span className="block text-xs text-gray-400 mt-1">My account ▾</span></span></button>{menu === "profile" && <div className="absolute right-0 z-30 mt-2 w-52 rounded-xl border border-gray-100 bg-white p-2 shadow-lg"><div className="border-b px-3 py-2"><p className="font-medium text-gray-800">{user.name}</p><p className="truncate text-xs text-gray-400">{user.email}</p></div><Link onClick={() => setMenu(null)} to="/profile" className="block rounded-lg px-3 py-2 text-sm hover:bg-brand-50">My profile</Link><Link onClick={() => setMenu(null)} to="/bookings" className="block rounded-lg px-3 py-2 text-sm hover:bg-brand-50">My bookings</Link>{["vendor", "admin"].includes(user.role) && <Link onClick={() => setMenu(null)} to="/vendor/onboarding" className="block rounded-lg px-3 py-2 text-sm hover:bg-brand-50">Vendor dashboard</Link>}<button onClick={signOut} className="mt-1 w-full rounded-lg px-3 py-2 text-left text-sm text-red-600 hover:bg-red-50">Sign out</button></div>}</div> : <Link to="/login" className="bg-brand-600 hover:bg-brand-700 text-white px-4 py-2 rounded-lg font-medium">Sign in</Link>}</div></header>;
}
